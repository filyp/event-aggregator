{
  "name": "event-aggregator",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy data/site --project-name=krk-events"
  },
  "dependencies": {
    "facebook-event-scraper": "^0.2.7"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
